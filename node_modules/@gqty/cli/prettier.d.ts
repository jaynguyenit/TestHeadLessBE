import * as deps from './deps.js';
export declare function formatPrettier(defaultOptions: Omit<deps.PrettierOptions, 'parser'> & Required<Pick<deps.PrettierOptions, 'parser'>>): {
    format(input: string): Promise<string>;
};
